# SBox AMP template

Unofficial AMP Generic Module template for the s&box dedicated server.

AMP repository entry:

```txt
hydrocut/sbox-amp-template:main
```

After changing files in GitHub, go to AMP > Configuration > Instance Deployment > Fetch Latest.

The application display name is intentionally `SBox Dedicated Server` so it is easier to find in AMP by searching `sbox`.
